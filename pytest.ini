[pytest]
pythonpath = app
testpaths = tests
addopts = --strict-markers
markers =
    characterization: Locks down existing SolarInspector behavior before refactoring.
    integration: Exercises multiple application components or local protocol integration.
    release: Covers update, upgrade, packaging, and release workflows.
